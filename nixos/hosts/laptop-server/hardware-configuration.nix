{ config, lib, pkgs, modulesPath, ... }:

{
  imports = [ ];

  # Replace this file with the generated laptop-specific config after installing:
  #   sudo cp /etc/nixos/hardware-configuration.nix /path/to/dotfiles/nixos/hosts/laptop-server/
  #
  # The generated file normally contains fileSystems, swapDevices, kernel modules,
  # CPU microcode, and other hardware-specific settings.
}
