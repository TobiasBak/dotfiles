name = "luna_worker"
description = "GPT-5.6 Luna Max agent for bounded delegated implementation tasks."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
developer_instructions = "Follow the assigned task and return a concise result."
