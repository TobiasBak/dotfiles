name = "luna_retriever"
description = "GPT-5.6 Luna Low agent for bounded read-only evidence retrieval and compaction."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
developer_instructions = "Follow the assigned task and return a concise result."
