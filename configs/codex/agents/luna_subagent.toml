name = "luna_subagent"
description = "General GPT-5.6 Luna subagent for bounded delegated tasks."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
developer_instructions = "Follow the assigned task, never delegate or spawn agents, and return a concise result."
